/*async function process() {
    const first = await Promise.resolve("1");
    const second = await Promise.resolve("2");
    console.log(first);
    console.log(second);
}
process();
const addNumber = async (a, b) => {
    return a+b
}

console.log(addNumber(5, 5))
*/
try {
    let x = y + 5;
} catch (error) {
    console.log("y is not defined");
}